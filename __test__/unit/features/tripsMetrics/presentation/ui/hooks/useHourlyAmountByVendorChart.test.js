import {test, describe} from "vitest";

describe('', ()=>{
    // Renders a bar chart with given labels and records
    test('should render a bar chart with given labels and records', () => {
        // document.body.innerHTML = '<canvas id="trips"></canvas>';
        // const labels = ['January', 'February'];
        // const records = [[10, 20], [30, 40]];
        // const chart = require('/src/features/tripsMetrics/presentation/ui/hooks/useHourlyAmountByVendorChart.js').default;
        //
        // chart(labels, records);
        //
        // const ctx = document.querySelector('#trips').getContext('2d');
        // expect(ctx).not.toBeNull();
        // expect(Chart).toHaveBeenCalledWith(ctx, expect.objectContaining({
        //     type: 'bar',
        //     data: {
        //         labels,
        //         datasets: expect.any(Array),
        //     },
        //     options: expect.any(Object),
        // }));
    });
})